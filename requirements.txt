# ==========================================================
# Hauptbibliotheken für die Anwendung
# ==========================================================
customtkinter==5.2.2
Pillow==11.3.0
python-docx==1.2.0
fpdf2==2.8.4

# ==========================================================
# Optionale Bibliotheken für zusätzliche Funktionen
# (Die App funktioniert auch ohne diese, aber mit eingeschränktem Funktionsumfang)
# ==========================================================
accessible-output2==0.17      # Für Screenreader-Unterstützung
pyspellchecker==0.8.1         # Für die Rechtschreibprüfung
language-tool-python          # Für die Grammatikprüfung
matplotlib==3.10.6            # Für die Diagramme in der Statistik-Ansicht
tkcalendar==1.6.1             # Für die Kalender-Widgets

# ==========================================================
# Bibliotheken für die Entwicklung (optional)
# ==========================================================
pytest==8.2.2                 # Zum Ausführen der Tests
pyinstaller==6.15.0           # Zum Erstellen der .exe-Datei

# To install the dependencies:
# pip install -r requirements.txt

# To prevent creation of .pyc files:
# [Environment]::SetEnvironmentVariable("PYTHONDONTWRITEBYTECODE", "1", "User")
